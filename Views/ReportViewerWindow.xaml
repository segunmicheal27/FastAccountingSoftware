<Window
    x:Class="FastAccountingSoftware.Views.ReportViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Financial Report Viewer"
    Height="600" Width="550"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="#FAF6EE"
    BorderThickness="1"
    BorderBrush="#E5E1D6"
    FontFamily="Segoe UI Variable, Inter, Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Footer Actions -->
        </Grid.RowDefinitions>

        <!-- Dark Green Header -->
        <Border Grid.Row="0" Background="#1B2A1C" Padding="28">
            <StackPanel>
                <TextBlock x:Name="ReportTitleText" Text="PROFIT &amp; LOSS STATEMENT" FontSize="20" FontWeight="Bold" Foreground="#FF7A00"/>
                <TextBlock x:Name="ReportPeriodText" Text="For the period ending today" FontSize="13" Foreground="#88FFFFFF" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Report Body Scrollable -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="28">
            <StackPanel x:Name="ReportContentArea">
                <!-- Content injected dynamically via C# code-behind -->
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Actions -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E5E1D6" BorderThickness="0,1,0,0" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="Print Report" Click="Print_Click" Background="Transparent" Foreground="#64748B" BorderThickness="0" HorizontalAlignment="Left" Padding="12,8"/>
                <Button Grid.Column="1" Content="Close" Click="Close_Click" Background="#1E293B" Foreground="White" BorderThickness="0" Padding="24,8" />
            </Grid>
        </Border>
    </Grid>
</Window>
